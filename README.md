# Liberu Billing Provisioning

Provider-neutral service provisioning with explicit lifecycle state, idempotent transitions, queued-operation evidence, rollback, polling, and reconciliation boundaries.

The package owns its persistence and exposes typed actions and queries. Provider SDKs and presentation adapters belong in separate packages.
